#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
<<<<<<< HEAD
  exec "$basedir/node"  "$basedir/../json-server/lib/cli/bin.js" "$@"
else 
  exec node  "$basedir/../json-server/lib/cli/bin.js" "$@"
fi
=======
  "$basedir/node"  "$basedir/../json-server/lib/cli/bin.js" "$@"
  ret=$?
else 
  node  "$basedir/../json-server/lib/cli/bin.js" "$@"
  ret=$?
fi
exit $ret
>>>>>>> 8638576a8fa1562e7be508dabf57500441d03dcc
